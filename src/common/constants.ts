export enum LayoutModes {
    Mobile = 'mobile',
    Compact = 'compact',
    Extended = 'extended'
}

export enum KeyCodes {
    ArrowRight = 39,
    ArrowLeft = 37,
    ArrowUp = 38,
    ArrowDown = 40,
    Space = 32,
    Enter = 13
}

export enum WindowOrientation {
    Portrait = 0,
    Landscape = 90,
    Desktop = 360
}